{
  "$schema": "https://json-schema.org/schema",
  "$id": "Section",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the application",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the demo application?"
    },
    "application": {
      "type": "string",
      "description": "Application will this Section be used for.",
      "x-prompt": "Which application will this Section be used for?",
      "x-dropdown": "projects"
    },
    "applicationName": {
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "description": "Override the applicationName from application input.",
      "x-prompt": "Used for prefixing names?"
    },
    "applicationNameShort": {
      "type": "string",
      "description": "Will be used prefix class names to avoid importing the wrong files.",
      "x-prompt": "How to prefix class names so we know what app ther're connected to?"
    },
    "directory": {
      "type": "string",
      "description": "Directory where the application is placed",
      "x-prompt": "In which directory should the application be generated?"
    }
  },
  "required": [
    "name", "application", "applicationNameShort"
  ]
}