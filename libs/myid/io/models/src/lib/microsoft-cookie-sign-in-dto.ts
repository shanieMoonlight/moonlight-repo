//TODO: Upate this file once Microsoft sign-in are implemented (the fields are probably incorrect)
export interface MicrosoftCookieSignInDto {
  id?: string;
  authToken: string;
  subscriptionPlanId?: string;
  deviceId?: string;
  rememberMe?: boolean;
}
