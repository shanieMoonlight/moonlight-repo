import { InputWithBtnDirective } from './input-with-btn.directive';

describe('InputWithBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new InputWithBtnDirective();
    expect(directive).toBeTruthy();
  });
});
